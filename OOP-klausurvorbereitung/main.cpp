#include <iostream>
#include <iomanip>
#include "datum2.hpp"
using namespace std;


list.erase(next(list.begin(),tmp))

int main(){
	for(int& it, list){
		int i = &it -&v[0];
	}
	for_each(list.begin(),list.end(),[&list,&search](auto const& it){
		auto i = &it - &v[0];
	});

	vector<int>::iterator it;
	for(it = v.begin();it < v.end();it++){
		auto i = distance(v.begin(),it);
	}

}

class Divisonexecption: public exception{
	private:
		const char* message;
	public:
		Divsionexeption(const char* m): message(m) {}
		void const char* what() const throw() override{
			return message;
		}

	try{
		if(n!=n){
			throw Divisionexception("custom message");
		}catch (Divisionexception& error){
			cout << error.what() << endl;
			exit(1);
		}
	}
};

class Singelton{
	private:
		static Singelton* obj;

		Singelton(){
			obj++;
		}
	public:
		static Singelton* get(){
			if (obj == 0){
				obj  = new Singelton();
			}
			return obj;
		}
};
Singelton* Singelton::obj=0;

arr++
Punkt tmp = new Punkt[arr];
memcpy(tmp,punkt,sizeof(Punkt)*arr)
delete[] punkt;
punkt = tmp
punkt[arr-1] = value;

list.erase(next(list.begin(),tmp))

vector 
list
for(int& it, list){
	int i = &it - &list[0];
}
vector<int>::iterator it;
for(it = v.begin(); it < v.end(); it++){
	auto i = distance(v.begin(),tmp)
}

for_each(v.begin(),v.end(),[&v,&find](auto const& it){
	auto i = &it - &v[0];
});